import React from 'react';
import * as ReactDOM from 'react-dom';
import Types from './pages/poke-types';
import './styles.css';

const mountNode = document.getElementById("app");
ReactDOM.render(<Types />, mountNode);
